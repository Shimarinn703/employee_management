<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jhc.employee_management.mapper.SkillAnswerMapper">

    <!-- Base_Column_List -->
    <sql id="Base_Column_List">
        id, question_id, content, like_count, creator_id, created_at, updated_at
    </sql>

    <!-- BaseResultMap -->
    <resultMap id="BaseResultMap" type="com.jhc.employee_management.entity.SkillAnswer">
        <id column="id" property="id"/>
        <result column="question_id" property="questionId"/>
        <result column="content" property="content"/>
        <result column="like_count" property="likeCount"/>
        <result column="creator_id" property="creatorId"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <!-- INSERT -->
    <insert id="insert" parameterType="com.jhc.employee_management.entity.SkillAnswer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO skill_answer (question_id, content, like_count, creator_id, created_at, updated_at)
        VALUES (#{questionId}, #{content}, #{likeCount}, #{creatorId}, #{createdAt}, #{updatedAt})
    </insert>

    <!-- SELECT by ID -->
    <select id="selectById" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM skill_answer
        WHERE id = #{id}
    </select>

    <!-- SELECT by Question -->
    <select id="selectByQuestionId" parameterType="long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM skill_answer
        WHERE question_id = #{questionId}
        ORDER BY created_at ASC
    </select>

    <!-- UPDATE LikeCount -->
    <update id="updateLikeCount">
        UPDATE skill_answer
        SET like_count = #{likeCount}, updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- DELETE by ID -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM skill_answer WHERE id = #{id}
    </delete>

</mapper>
