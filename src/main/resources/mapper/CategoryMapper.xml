<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.jhc.employee_management.mapper.CategoryMapper">

    <resultMap id="BaseResultMap" type="com.jhc.employee_management.entity.Category">
        <id column="id" property="id" />
        <result column="category_name" property="categoryName" />
        <result column="description" property="description" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
    </resultMap>

    <sql id="Base_Column_List">
        id, category_name, description, created_at, updated_at
    </sql>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List" />
        FROM category
        WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.jhc.employee_management.entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (
            category_name, description, created_at, updated_at
        ) VALUES (
            #{categoryName}, #{description}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateById" parameterType="com.jhc.employee_management.entity.Category">
        UPDATE category
        <set>
            <if test="categoryName != null and categoryName != ''">category_name = #{categoryName},</if>
            <if test="description != null and description != ''">description = #{description},</if>
            <if test="updatedAt != null">updated_at = #{updatedAt},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM category
        WHERE id = #{id}
    </delete>
    
</mapper>